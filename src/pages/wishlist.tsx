import WishListProperties from "../components/wishListProperties/wishListProperties";

const Wishlist = () => {
  return (
    <div className="main_wrapper">
      <WishListProperties />
    </div>
  );
};

export default Wishlist;
